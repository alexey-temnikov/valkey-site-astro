---
// Starlight Header override — renders the shared marketing SiteHeader so every
// page (marketing and docs) uses the same navigation chrome AND the same ⌘K
// SearchOverlay (mounted inside SiteHeader). No need for Starlight's own
// <Search> widget anymore: SearchOverlay handles ⌘K for the whole site
// against the site-wide Pagefind index at /pagefind/.
import SiteHeader from "../SiteHeader.astro";
---

<SiteHeader />
